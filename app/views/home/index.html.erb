
<div class="card card-backside card-site-title">

  <div class="" style="text-align: center; margin-top: 155px;">

  </div>
</div>

<div class="card card-about">

<h2 class="about-heading">About</h2>

<p>
<%= Location.free_workspaces %> free work spaces are currently available for rent. 
Monthly rent is now <%= Invoice.new.taxless.to_i %> € (+VAT).
</p> 

<p>
  Services include 200 MB internet, electronic alarm system, weekly cleaning, kitchen area and planning/meeting space.
</p>

<p>
Deck is located at Pursimiehenkatu 26&nbsp;H, right next to the last stop of tram number 1A (Eira). Bus 14 rides rides very close as well.
</p>

<p>
  For enquiries please contact Pirkka&nbsp;Esko.
</p>

</div>


<div class="stack">

  <% @members.shuffle.each do |member| %>
  <div class="card member-card unselectable" style="<%= random_rotation_css %>">
    <a href="<%= member.url %>">
      <div class="member-name">
        <%= member.name %>
      </div>
      <div class="member-company">  
        <%= member.company%>
      </div>
      </a>
  </div>
  <% end %>

</div>

<div class="stack">

  <% @members.shuffle.each do |member| %>
  <div class="card member-card unselectable" style="<%= random_rotation_css %>">
    <a href="<%= member.url %>">
      <div class="member-name">
        <%= member.name %>
      </div>
      <div class="member-company">  
        <%= member.company%>
      </div>
      </a>
  </div>
  <% end %>

</div>

<div id="map-card" class="card card-map">
  <div id="map" class="map"></div>
</div>


<script type="text/javascript">
$(function () {
  
  // stamen layer
  var layer = new L.StamenTileLayer("toner-lite");
  var map = new L.Map("map", {
      center: new L.LatLng(<%= Location.lat%>, <%= Location.lon %>),
      zoom: 17
  });
  map.addLayer(layer);
  
  var deckIcon = L.icon({
    iconUrl: '/assets/deck-marker.png',
    iconSize:     [80, 47],
    iconAnchor:   [40, 0],
    popupAnchor:  [-3, -76]
  });  
  
  // add a marker in the given location, attach some popup content to it and open the popup
  L.marker([<%= Location.lat%>, <%= Location.lon %>], {icon: deckIcon}).addTo(map);
  
    });
</script>