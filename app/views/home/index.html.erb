<div class="" style="text-align: center;">

<%= image_tag 'deck-logo.png', :style => '' %>


<div style="margin: 30px;">co-working space for software pros</div>

</div>

<h2>About</h2>

<p>
There are <%= Location.free_workspaces %> work spaces currently available for renting. Monthly rent is now <%= Invoice.new.taxless.to_i %> € (+VAT).
</p> 

<p>
  Services include 200 MB internet, electronic alarm system, weekly cleaning, kitchen area and planning/meeting space.
</p>

<h2>Members</h2>

<ul class="thumbnails">
  <% @members.each do |member| %>
    <li class="span4">
      <a href="<%= member.url %>" class="thumbnail">
      <img data-src="holder.js/300x200" alt="">
      <div class="member-name">
        <%= member.name %>
      </div>
      <div class="member-company">  
        <%= member.company%>
      </div>
      </a>
    </li>
  <% end %>
</ul>

<h2>Location</h2>

<div id="map" class="map" style="height: 300px"></div>

<script type="text/javascript">
$(function () {
  
  // create a map in the "map" div, set the view to a given place and zoom
  var map = L.map('map').setView([<%= Location.lat%>, <%= Location.lon %>], 16);

  // add an OpenStreetMap tile layer
  L.tileLayer('http://{s}.tile.osm.org/{z}/{x}/{y}.png', {
      attribution: '&copy; <a href="http://osm.org/copyright">OpenStreetMap</a> contributors'
  }).addTo(map);

  // add a marker in the given location, attach some popup content to it and open the popup
  L.marker([<%= Location.lat%>, <%= Location.lon %>]).addTo(map)
      .bindPopup('deck is located!')
      .openPopup();
  
    });
</script>